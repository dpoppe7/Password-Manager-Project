package com.example.passwordmanagerproject.viewmodel

/*
import android.app.Application
import androidx.lifecycle.AndroidViewModel
import androidx.lifecycle.LiveData
import com.example.passwordmanagerproject.model.Account
import com.example.passwordmanagerproject.repo.UserDataRepository

class AccountViewModel(application: Application): AndroidViewModel(application) {

    private val accountRepo = UserDataRepository.getInstance(application.applicationContext)

    val accountListLiveData: LiveData<List<Account>> = accountRepo.getAccounts()

    fun addAccount(account: Account) = accountRepo.addAccount(account)

    fun deleteAccount(account: Account) = accountRepo.deleteAccount(account)
}*/
